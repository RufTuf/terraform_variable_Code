name: terraform deployment

on: 
  push: 
    branches: 
      - main

workflow_dispatch:

env:
  TF_VERSION: 1.8.5

jobs:

  terraform-plan Plan
    name: Terraform Plan
    runs-on: self-hosted

    steps:

      - name: Checkout Code
        uses: actions/checkout@4

      - name: Setup Teraform
        uses: harsicorp/setup-terraform@v3
        with:
          terraform_vesrion: ${{
  
